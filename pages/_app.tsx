import '../styles/dist/global.css'
import type { AppProps } from 'next/app'

function pcp_peripherals({ Component, pageProps }: AppProps) {
  return <Component {...pageProps} />
}

export default pcp_peripherals
